# Agent Proposals

This directory stores the proposals/configurations for agents that the Kusanagi web server will control or reference to improve the k3s cluster.

## Structure
(Define your agent manifest structure here, e.g., JSON or YAML files)
